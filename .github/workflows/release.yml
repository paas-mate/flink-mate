name: Release

on:
  push:
    branches:
      - release-1-12-7

permissions: write-all

jobs:
  push_to_registries:
    name: Push Docker image to multiple registries
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repo
        uses: actions/checkout@v3
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - name: Push to Docker Hub
        uses: docker/build-push-action@v4
        with:
          push: true
          tags: ttbb/flink:mate-1-12-7
          platforms: linux/amd64,linux/arm64
      - name: Save Docker Images
        run: docker pull ttbb/flink:mate-1-12-7;docker save ttbb/flink:mate-1-12-7 -o flink-mate-1-12-7.tar.gz
      - name: Release
        uses: shoothzj/latest-release-action@v1
        with:
          tag_name: v1.12.7
          release_name: v1.12.7
          files: flink-mate-1-12-7.tar.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
